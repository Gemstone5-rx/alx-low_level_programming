#include <stdio.h>

/**
 * main - Entry point
 *
 * char: Variable character declaration and initialization
 *
 * puts: writes the string
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch[] = "\"Programming is like building a multilingual puzzle";

	puts(ch);

	return (0);
}
